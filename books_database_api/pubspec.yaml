name: books_database_api
publish_to: 'none'

environment:
  sdk: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_html: ^3.0.0
  http: ^1.4.0
  intl: ^0.20.2
  platform_adaptivity:
    git:
      url: git@github.com:LathDevers/flutter_packages.git
      path: platform_adaptivity

flutter:
  assets:
    - images/